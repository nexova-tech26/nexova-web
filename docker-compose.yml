version: '3.8'

services:
  backend:
    build: ./backend
    container_name: nexova_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: nexova_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always